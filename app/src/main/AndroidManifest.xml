<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rentandsharebikes">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BikesImageRentBikesCustomer"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyA2v62ZDMh39bY5Wg1FYxOLE4LNeDJCOHU" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".UpdateBikeStoreDetails" />
        <activity android:name=".UpdateBikeDetails" />
        <activity android:name=".BikeStoreImageShowBikesListAdmin" />
        <activity android:name=".BikeStoreImageShowStoresListCustomer" />
        <activity android:name=".BikeStoreImageAddBikesAdmin" />
        <activity android:name=".BikeStoreImageShowStoresListAdmin" />
        <activity android:name=".BikeStoreImageRentBikesCustomer" />
        <activity android:name=".EditCustomerProfile" />
        <activity android:name=".ChangePassword" />
        <activity android:name=".CalculateCoordinates" />
        <activity android:name=".ResetPassword" />
        <activity android:name=".RentBikesCustomer" />
        <activity android:name=".AdminPage" />
        <activity android:name=".CustomerPage" />
        <activity android:name=".AddBikes" />
        <activity android:name=".AddBikeStore" />
        <activity android:name=".LoginCustomer" />
        <activity android:name=".RegisterCustomer" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>